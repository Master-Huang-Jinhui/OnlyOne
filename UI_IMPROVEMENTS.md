# Only One UI 改进方案

> 参考喜茶/奈雪的茶/万波茶饮/BobaStraw 等品牌设计语言
> 原则：不引入新框架，基于现有 Tailwind CSS 优化

## 一、整体设计语言升级

### 1. 配色方案（当前：浅蓝+白 → 建议：奶白+暖棕+品牌绿）

| 用途 | 当前 | 建议 | 说明 |
|------|------|------|------|
| 页面背景 | #ffffff | #FAF7F2 | 奶白色，避免纯白的冷漠感 |
| 卡片背景 | #ffffff | #FFFEFC | 微暖白 |
| 品牌主色 | 蓝色系 | #2D5A27 深绿 | 呼应茶叶/健康，与logo绿色一致 |
| 品牌辅色 | - | #C4A77D 暖棕 | 奶茶色，用于点缀和按钮 |
| 强调色 | - | #E8B84B 琥珀金 | 新品/推荐标签 |
| 文字主色 | #1f2937 | #2D2A26 | 暖黑，不刺眼 |
| 文字次色 | #6b7280 | #8B8378 | 暖灰 |
| 分割线 | #e5e7eb | #EDE6DB | 暖灰线 |

**实现方式**：修改 `tailwind.config.js` 的 colors 配置，新增 `cream`、`brand`、`tea` 色板。

### 2. 字体排印

- **标题字体**：引入衬线字体（如 Playfair Display 或 Noto Serif SC），用于首页大标题、区块标题，增加品牌高级感
- **正文字体**：保持现有无衬线（Inter / 系统默认）
- **数字字体**：价格、订单号使用等宽数字（tabular-nums），对齐更整齐
- **字号层级**：H1 从 2rem 提升到 2.5rem，增加视觉冲击力

### 3. 圆角与阴影

- 卡片圆角：从 `rounded-lg`(8px) 提升到 `rounded-2xl`(16px)，更圆润友好
- 按钮圆角：`rounded-xl`(12px)
- 图片圆角：商品图片 `rounded-xl`
- 阴影：使用更柔和的暖色阴影 `shadow-[0_4px_20px_rgba(45,42,38,0.08)]`，替代默认冷色阴影

## 二、前台首页改进

### 1. 导航栏
- **当前**：白底+文字链接
- **改进**：
  - 滚动时导航栏变为毛玻璃效果（backdrop-blur-md + bg-white/80）
  - Logo 左侧，导航居中，"立即点餐"按钮右侧（品牌色填充）
  - 移动端汉堡菜单展开时全屏覆盖，带淡入动画

### 2. 首屏轮播图
- **当前**：全宽图片轮播
- **改进**：
  - 左侧文字+右侧图片的分屏布局（桌面端），移动端上下布局
  - 大标题使用衬线字体，副标题描述品牌
  - "查看菜单"CTA按钮，带悬停微动效（箭头右移）
  - 轮播指示器改为小圆点，当前页放大

### 3. 新品上市区块
- 卡片横向滚动（mobile snap），桌面端网格
- 卡片悬停时图片微放大（scale-105）+ 阴影加深
- "新品"角标使用琥珀金色，左上角斜贴效果

### 4. 品牌故事/茶品溯源/工艺区块
- 交替左右布局（图左文右 → 文左图右），增加节奏感
- 区块间大留白（py-20 以上）
- 标题下方加品牌色短横线装饰（w-12 h-1 bg-brand rounded-full）
- 茶品卡片：悬停时上浮（-translate-y-1）+ 阴影，图片圆形裁剪

### 5. 奶茶制作器
- 已有Lottie动画，保持
- 周围增加装饰性茶叶SVG背景（低透明度）
- 步骤按钮改为胶囊形，选中态品牌色填充

### 6. 页脚
- 深色背景（#2D2A26），奶白文字
- 四列布局：品牌信息、营业时间、联系方式、快速链接
- 底部版权行，社交图标

## 三、菜单页面改进

### 1. 分类导航
- **当前**：顶部横向标签
- **改进**：
  - 桌面端：左侧固定分类栏（sticky），点击平滑滚动到对应分类
  - 移动端：顶部横向滚动标签，当前分类品牌色下划线
  - 分类图标+文字，更直观

### 2. 商品卡片
- **当前**：简单列表
- **改进**：
  - 卡片式布局，左图右文（桌面端两列）
  - 商品图片圆角，悬停微放大
  - 价格使用品牌色加粗，字号略大
  - "推荐"标签：琥珀金色小角标
  - 加号按钮：圆形品牌色，悬停放大，点击有波纹反馈
  - 已加数量：按钮变为"− N +"步进器
  - 售罄商品：灰度处理 + "售罄"标签覆盖

### 3. 口味选择
- 弹层改为底部抽屉（Bottom Sheet），从下方滑入
- 大类标题加粗，选项圆形/圆角按钮
- 选中态品牌色边框+填充
- 加价选项显示 "+$1.00"
- 底部固定"加入购物车"按钮

## 四、购物车改进

### 1. 购物车入口
- 右下角悬浮购物车按钮（圆形品牌色），带数量角标
- 点击从右侧滑出抽屉（Drawer），而非跳转页面
- 遮罩层点击关闭

### 2. 购物车内容
- 商品条目：缩略图+名称+口味标签+价格+步进器
- 口味标签：小胶囊形，不同颜色区分
- 备注：可展开编辑的文本框
- 取餐方式：三选一卡片（堂吃/自取/配送），选中态品牌色
- 金额明细：小计、税费、配送费、合计，右对齐
- 底部固定"去结算"按钮，全宽品牌色

## 五、后台管理改进

### 1. 侧边栏
- 当前浅蓝保持，增加夜间模式（已有）
- 菜单项图标+文字，选中态左侧品牌色竖条
- 一级菜单可折叠，二级菜单缩进
- 底部用户信息+退出按钮

### 2. 数据卡片
- 统计卡片：图标+数值+趋势（↑/↓百分比）
- 卡片悬停轻微上浮
- 热销商品：横向条形图，品牌色渐变
- 订单状态：彩色标签，可点击筛选

### 3. 表格
- 表头暖灰背景，行悬停奶白高亮
- 操作按钮：图标按钮，悬停显示文字提示
- 分页：底部居中，页码按钮圆角
- 空状态：插画+文字+操作按钮

### 4. 表单/弹窗
- 弹窗圆角2xl，标题左侧品牌色竖条
- 输入框：圆角xl，聚焦时品牌色边框+光晕
- 按钮：主按钮品牌色填充，次按钮描边
- 开关：品牌色滑块

## 六、员工端改进

### 1. 欢迎界面
- 大标题衬线字体，"欢迎回来，{名字}"
- 两个大按钮：堂吃/打包，图标+文字，卡片式
- 打卡按钮：右上角小按钮
- 背景：奶白色，底部装饰性茶叶纹样

### 2. 点餐界面
- 左侧分类（可折叠），中间商品网格，右侧购物车（三栏布局）
- 商品卡片：大图+名称+价格，点击直接加入
- 购物车：实时显示，可编辑数量和备注
- 底部：桌号/取餐号 + 合计 + "下单"按钮
- 口味选择：右侧滑出面板，不遮挡商品

### 3. 订单详情
- 大字号显示桌号/取餐号
- 商品列表：名称+数量+口味标签+备注
- 状态时间线：下单→制作中→已完成，带时间戳
- 底部操作按钮：推进状态/取消

## 七、动效与交互

### 1. 页面过渡
- 路由切换时淡入（fade-in 200ms）
- 列表项进入时交错淡入上移（stagger animation）

### 2. 按钮反馈
- 点击时 scale-95（缩小反馈）
- 悬停时 -translate-y-0.5 + 阴影加深
- 主按钮品牌色渐变，悬停时渐变方向变化

### 3. 卡片悬停
- 图片 scale-105（缓慢放大）
- 卡片 -translate-y-1 + 阴影
- 过渡时长 300ms，缓动 ease-out

### 4. 加载状态
- 骨架屏（Skeleton）：商品卡片、表格行
- 加载动画：品牌色脉冲

## 八、优先级与实施建议

| 优先级 | 改进项 | 预计工作量 |
|--------|--------|-----------|
| P0 | 配色方案升级（tailwind.config） | 小 |
| P0 | 商品卡片 redesign（菜单页） | 中 |
| P0 | 购物车抽屉化 | 中 |
| P1 | 首页导航毛玻璃+分屏轮播 | 中 |
| P1 | 后台表格/卡片样式优化 | 小 |
| P1 | 按钮/输入框统一样式 | 小 |
| P1 | 员工端三栏布局 | 中 |
| P2 | 衬线标题字体引入 | 小 |
| P2 | 区块交替布局+装饰元素 | 中 |
| P2 | 骨架屏加载 | 中 |
| P2 | 页面过渡动效 | 小 |

**建议实施顺序**：先改配色和基础组件（P0），再改菜单和购物车（用户最常用），最后优化首页和后台。
