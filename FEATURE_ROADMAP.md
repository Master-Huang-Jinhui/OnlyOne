# Only One 系统功能路线图

> 基于 Toast / Square / 客如云 / 美团等主流点餐系统对比整理
> 优先级：P0=核心必备, P1=重要提升, P2=锦上添花

## 一、厨房与出餐

### 1. KDS 厨房显示系统 (P1)
- **现状**：订单仅在后台列表显示，后厨需要打印小票或人工传递
- **竞品做法**：Toast/Square/客如云均有KDS，订单自动分发到对应厨位（烧烤区/饮品区），颜色预警超时订单
- **实现建议**：
  - 新建 `/kds` 页面，全屏显示待制作订单卡片
  - 按商品分类分区域显示（烧烤区/奶茶区/小食区）
  - 订单卡片显示桌号/取餐号、商品列表、备注、下单时间
  - 超时订单变红闪烁（如超过15分钟）
  - 点击"制作完成"按钮推进订单状态
  - 支持声音提醒（新订单叮咚声）
- **代码位置**：`frontend/src/pages/kds/` (新建), `backend/src/routes/orders.js`

### 2. 票据路由 (P2)
- **现状**：无
- **竞品做法**：不同类别的商品打印到不同打印机（烧烤打印机/奶茶打印机）
- **实现建议**：后台配置打印机，按商品分类路由

### 3. 订单限流 (P2)
- **现状**：无
- **竞品做法**：Toast支持订单限流，高峰期限制接单量避免后厨过载
- **实现建议**：后台设置同时制作上限，超过后前台提示"当前繁忙，请稍后"

## 二、库存与成本

### 4. 实时库存追踪 (P1)
- **现状**：货物管理只有手动记录库存，卖出商品不自动扣减
- **竞品做法**：每卖一道菜自动扣减原料库存（如卖1杯奶茶扣减珍珠10g、牛奶100ml）
- **实现建议**：
  - 商品表添加 `recipe` 字段（JSON，记录原料配方：goods_id + 用量）
  - 下单成功时自动扣减对应货物库存
  - 库存低于阈值时后台预警（红色标记）
  - 仪表盘显示"库存不足"提醒
- **代码位置**：`backend/src/routes/orders.js` (下单扣减), `backend/src/routes/inventory.js` (配方管理)

### 5. 菜品盈利能力分析 (P1)
- **现状**：profit_records 表已有基础，但未形成完整分析
- **竞品做法**：自动计算每道菜的成本、毛利、毛利率，识别高利润/低利润菜品
- **实现建议**：
  - 后台"销售统计"页面添加"盈利能力"标签页
  - 按菜品显示：售价、原料成本、毛利、毛利率、销量、总毛利
  - 排序：按毛利率从高到低，识别"亏本菜"
  - 图表：毛利率分布柱状图

### 6. 采购建议 (P2)
- **现状**：手动创建采购单
- **竞品做法**：根据库存和销量自动生成采购建议
- **实现建议**：根据近7天销量和当前库存，计算需要采购的货物清单

## 三、客户与营销

### 7. CRM 客户关系管理 (P1)
- **现状**：订单记录客户姓名电话，但无客户档案
- **竞品做法**：客户档案包含订单历史、消费总额、偏好、生日
- **实现建议**：
  - 新建 `customers` 表（id, name, phone, email, address, total_spent, order_count, last_order, birthday, note）
  - 下单时自动匹配/创建客户（按手机号）
  - 后台"客户管理"页面：客户列表、消费排行、详情页
  - 客户详情：历史订单、常点商品、消费趋势

### 8. 会员/忠诚度计划 (P2)
- **现状**：无
- **竞品做法**：积分系统（消费1美元=1积分），积分兑换商品/折扣，会员等级
- **实现建议**：
  - `customers` 表添加 points, level 字段
  - 下单完成自动累加积分
  - 前台结算时可使用积分抵扣
  - 后台设置积分规则和兑换商品

### 9. 优惠券/促销 (P2)
- **现状**：无
- **竞品做法**：满减券、折扣券、新品试吃价、限时优惠
- **实现建议**：
  - 新建 `coupons` 表（code, type, value, min_order, start_date, end_date, usage_limit, used_count）
  - 前台结算时输入优惠码
  - 后台管理优惠券

## 四、点餐与支付

### 10. 分账/并桌 (P2)
- **现状**：堂吃一桌一账单
- **竞品做法**：Toast支持分账（AA制）、并桌（两桌合并结账）、转桌
- **实现建议**：
  - 员工端订单详情添加"分账"按钮：按人数均分或按商品分
  - 添加"转桌"功能：将订单从A桌转到B桌
  - 添加"并桌"功能：合并两桌订单

### 11. 桌边点餐 (P2)
- **现状**：员工端点餐，顾客扫码只能看菜单
- **竞品做法**：顾客扫码直接点餐加购，服务员确认后送后厨
- **实现建议**：前台扫码页面添加"加入购物车"和"呼叫服务员"功能

### 12. 多种支付方式 (P2)
- **现状**：仅记录总额，无支付方式
- **竞品做法**：现金、信用卡、Apple Pay、Google Pay、微信、支付宝
- **实现建议**：订单表添加 payment_method 字段，结算时选择支付方式

### 13. 预订/预点 (P2)
- **现状**：无
- **竞品做法**：Square支持preorders，顾客提前下单指定取餐时间
- **实现建议**：前台添加"预点"选项，选择取餐时间，订单标记为预订单

## 五、报表与分析

### 14. 深度销售报表 (P1)
- **现状**：有基础销售统计
- **竞品做法**：日/周/月销售趋势、高峰时段分析、品类占比、同比环比
- **实现建议**：
  - 仪表盘添加销售趋势折线图（近7天/30天）
  - 高峰时段热力图（星期×小时）
  - 品类销售占比饼图
  - 导出Excel报表

### 15. 人工成本报表 (P2)
- **现状**：有打卡记录，但无成本计算
- **竞品做法**：人工支出与销售对比，工时统计，时薪计算
- **实现建议**：
  - 员工表添加 hourly_wage 字段
  - 根据打卡记录计算工时和工资
  - 报表：人工成本占销售额比例

## 六、系统与稳定

### 16. 离线模式 (P2)
- **现状**：必须联网
- **竞品做法**：Toast有离线备份，断网时继续接单，联网后同步
- **实现建议**：Service Worker缓存，本地IndexedDB存储离线订单

### 17. 数据自动备份 (P1)
- **现状**：手动复制data.db
- **竞品做法**：每日自动备份到云存储
- **实现建议**：
  - 后端定时任务（node-cron）每日凌晨备份data.db到 backups/ 目录
  - 保留最近7天备份
  - 后台"系统设置"添加"立即备份"和"下载备份"按钮

### 18. 操作日志查看 (P2)
- **现状**：有审计日志写入文件，但无查看界面
- **实现建议**：后台添加"操作日志"页面，查看谁在什么时候做了什么

## 七、多门店扩展

### 19. 多门店管理 (P3)
- **现状**：单门店
- **竞品做法**：Toast支持多门店，总部统一管理菜单/库存/报表
- **实现建议**：添加 stores 表，所有业务表关联 store_id，总部视图切换门店

---

## 优先级汇总

| 优先级 | 功能 | 预计工作量 |
|--------|------|-----------|
| P1 | KDS厨房显示系统 | 中 |
| P1 | 实时库存追踪 | 中 |
| P1 | 菜品盈利能力分析 | 小 |
| P1 | CRM客户关系管理 | 中 |
| P1 | 深度销售报表 | 小 |
| P1 | 数据自动备份 | 小 |
| P2 | 票据路由 | 小 |
| P2 | 订单限流 | 小 |
| P2 | 采购建议 | 小 |
| P2 | 会员忠诚度 | 中 |
| P2 | 优惠券促销 | 中 |
| P2 | 分账并桌 | 中 |
| P2 | 桌边点餐 | 中 |
| P2 | 多种支付 | 小 |
| P2 | 预订预点 | 中 |
| P2 | 人工成本报表 | 小 |
| P2 | 离线模式 | 大 |
| P2 | 操作日志查看 | 小 |
| P3 | 多门店管理 | 大 |
