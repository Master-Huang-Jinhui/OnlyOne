# Only One 环境变量配置
# 复制此文件为 .env 并修改值，.env 不会被提交到 git

# 服务端口（默认3000）
PORT=3000

# JWT 密钥（生产环境务必修改为随机字符串，至少32位）
# 生成方法：node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=onlyone-secret-key-2024-change-in-production
